# ============================================================
# Run
# ============================================================
experiment_name: "cql_discrete_stable_bigmatrix"
output_root: "../runs"
seed: 0

# ============================================================
# Data
# ============================================================
train_npz: ../data/processed/big_matrix_kuairec_20260305_185345/rl_train.npz

reward_scale: 0.1

gamma: 0.95

alpha_cql: 0.1
cql_temperature: 5.0

epochs: 20
batch_size: 4096
lr_q: 0.0001
weight_decay: 0.0
grad_clip_norm: 5.0

tau_polyak: 0.002
target_update_period: 50

hidden1: 512
hidden2: 512

huber_delta: 1.0
max_q_abs: null

log_every_epochs: 1